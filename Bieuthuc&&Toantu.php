<?php
    /**
     * Biểu thức và toán tử
     * 
     */
    $a = 1;
    $b = 5;
    $total = $a + $b + 10; // $a =  $b + 10 =>biểu thức
    // $a , $b,10 , $total: toán hạng
    // $a, $b,10 => toán hạng
    // +,= :Toán  tử
   

    /* Toán tử gán
    - Toán tử =
    */
    $age = 30; // cú pháp : $ten_bien = gia_tri;
    const _WEB_HOST_ROTT = 'http://vnexress.net';
    echo $age;
    echo '<br>';
    echo _WEB_HOST_ROTT;
    echo '<br>';

    // Toán tử .=
    $outputStr = 'Học lập trình';
    // $outputStr = $outputStr .' PHP';
    // Viết tắt 
    $outputStr .= ' PHP';
    echo $outputStr;
    echo '<br>';

    // Toán tử số học
    //  toán tử  cộng (+)
    $a = 10;
    $b = 20;
    $total = $a + $b;
    echo $total;
    echo '<br>';

    // Toán tử chia lấy phần dư
    $a = 15;
    $b = 10;
    $result = $a % $b;
    echo $result;

    // Toán tử tăng 1 đơn vị (++)
    $count = 0;
    

?>